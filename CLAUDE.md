# 專案協作備忘

給未來（含換電腦後）處理這個 repo 的 Claude Code 看的協作規則。這份檔案會隨 repo 走，不依賴任何單一電腦上的設定。

## 這是什麼

柯醒翰為了消防人員升官等考試，整理的考前衝刺筆記＋練習題網站，部署在 GitHub Pages（github.com/kozh1025/qiz.git，分支 main，網址 kozh1025.github.io/qiz）。核心練習題檔案是 `法條速判練習.html`（ADMIN 行政程序法202題 + CORRUPT 貪污治罪條例與刑法120題）。

## 標準規則

1. **完成後主動 push。** 不用每次都問，改完、驗證完就 commit + push origin/main。網站是即時部署的，本地沒推上去等於沒做完。

2. **法條全文檔 vs 課本實際範圍檔，不要混用。**
   - 全文參考（法條逐條原文，範圍比實際考試範圍大）：`corruption-crime-law.html`、`admin-procedure-law.html`
   - 實際教學範圍（比較窄，才是真正會考的）：`admin-procedure-textbook.html`、`admin-procedure-slides.html`、`行政程序法-考場抄寫骨幹.html`、`corruption-crime-textbook.html`、`刑法瀆職罪與貪污治罪條例-抄寫骨幹.html`
   - 寫新內容或判斷「這條算不算重點」時，要對照後者，不能只看前者有沒有收錄。之前把「聽證程序」誤判成涵蓋§54-66（其實課本只教§54）就是踩到這個坑。

3. **改題庫內容時，主題/命名一致性要讓位給法條考點本身。** 例如把 CORRUPT 題庫改成消防情境時，有一題本來就是在測試「農田水利會員工算不算公務員」（§2身分公務員的特殊公法人考點），硬改成消防局場景會讓這個考點消失，所以保留了機構設定、只加了消防關聯（灌溉兼消防蓄水）。改動前先確認：這題的法條考點是否綁定在特定情境設定上，綁定的話就不要為了統一風格犧牲掉。

4. **委派子代理處理大量題目時：最多同時開3個。** 且合併回主檔案後，一定要自己重新解析 HTML 檔案獨立驗證一次（citation範圍、重複題目、大前提/涵攝分割標記、結語標記），不能只信 agent 自己回報的驗證結果——這個專案至今零內容錯誤流入正式檔案，靠的就是這一步。子代理輸出請它們寫到檔案而不是聊天回覆（陣列一長就會被截斷）。

5. **柯醒翰本人會仔細審題**，抓過的問題包括：法條要件（原有期間 vs 剩餘期間）被混淆、「倒果為因」（引用§6-1/§10這類需要前置犯罪的條文，但情境只空泛斷言「涉犯圖利罪」沒有具體事實）。他說「這題怪怪的」時，值得認真查，不是誤報。

## `法條速判練習.html` 技術架構（未來做其他考科的練習題頁面可以直接沿用這套）

單一自包含 HTML 檔，無後端、無 build step，資料寫死在 `<script>` 裡的 JS 陣列，開啟即可用，適合這種考前衝刺工具。

**資料結構**：每個題庫是一個陣列（目前是 `ADMIN`、`CORRUPT`，做新科目就照樣加一個新陣列，如 `PLAN`、`CRISIS`），每一題是：
```js
{a: "§條號", q: "情境段落", problem: "問：具體問題句", body: "大前提段落。本案涵攝段落...\n\n結語：結論句"}
```
- `a`：答案（法條引用），點卡片前先蓋住，是「先蓋牌」機制的核心。
- `q`：情境事實。
- `problem`：緊接在情境下方、獨立一行的提問句（不是塞進 q 裡）。
- `body`：**大前提／涵攝／結語是同一個字串**，不是分開存的欄位——靠字串裡的標記由前端「機械分割」出來顯示，好處是資料零冗餘、以後要調整顯示邏輯不用動到內容本身：
  - 大前提／涵攝分界：body 裡找最早出現的 `"。本案"` / `"，本案"` / `"：本案"` / `"；本案"`（`splitPremise()`），切點之前是大前提、之後是涵攝。寫新題目時只要在情境轉折處自然帶出「本案」開頭，前端就會自動抓到。
  - 結語：body 結尾一定要有 `"\n\n結語："` 開頭的段落，跟涵攝隔一個空行。
  - 可選的補充模組（例如貪污題庫的「減刑與沒收檢視」§8/§12/§17檢查）：直接當作涵攝和結語中間的一個 `\n\n` 分隔段落插入即可，不需要額外欄位。

**「淡化次要內容」機制**（兩層，都是純顯示層，資料本身不用打任何標記）：
- 涵攝段落裡，句首出現「又／縱／倘／至若」的句子（`EXTRA_PREFIXES`），代表這是加分延伸辨析、不是拿到及格分數必寫的內容，`markExtra()` 會自動抓出來套 `.ans-extra` class（灰色 `#a8a49a`，**不要用斜體**，柯醒翰明確要求過）。
- 大前提段落裡，出現「，本款所稱／，本罪所稱／，本條所稱／。所謂／，所謂／，蓋」這些標記（`PREMISE_ELAB_MARKERS`）之後的內容，代表是定義說明或學理理由闡述（不是法條要件本身），`extractPremiseExtra()` 會切出來同樣套淡化樣式。這兩組標記清單以後可以視內容特性繼續加詞。
- 「建議完整書寫」黃色小標籤（`.ans-note`）：`skipExtraLength()` 算出「扣掉所有淡化內容後還剩多少字」，低於450字（約15-20分鐘寫作量）就自動顯示，提醒使用者這題光寫必寫內容份量不夠，考場應該連加分內容一起寫。這個閾值是跟柯醒翰討論過的，不要隨便改。

**主題分類篩選器**（每個題庫tab各自一組，放在tab按鈕下方、題目列表上方的 `.filter-row`）：
- 資料只在顯示層過濾，不動原始陣列——`FULL = {admin: ADMIN, corrupt: CORRUPT}` 保留完整資料，`state[key].data` 才是目前實際顯示的（全部或篩選後）子集，`state[key].filter` 記錄目前選的分類 key（`null` 代表全部）。
- 分類定義是一個陣列，每項 `{key, label, test}`：`label` 要把涵蓋的法條範圍寫進按鈕文字（例如「陳述意見與聽證（§54-66、§102-109）」），`test` 用來判斷一題屬不屬於這類。行政程序法用純數字條號區間判斷（`firstArticleNum()` 抓第一個引用條號），貪污/刑法這種有法規名稱前綴、條號帶「之1」變體的則直接用 regex 比對 `a` 欄位開頭。
- 分類數量抓 8-9 類為原則（原始細分可能到13-14類，同一法典章節、性質相近的再合併），每個按鈕文字後面附上「・N題」實際題數，`renderFilters()` 會自動算。
- 只用第一個（主要）法條引用判斷分類，多重引用題目（如「§111①、§117」）跟著主要引用歸類。
- 做新科目時複製 `ADMIN_CATEGORIES`／`CORRUPT_CATEGORIES` 的寫法，先把新題庫的法條引用分佈抓出來看數量分布，再決定怎麼合併到8-9類，不要憑感覺硬分。

**其他慣例**：
- 情境主角統一取名「小明」，次要/對造角色維持原本代稱（乙、丙、B、C、廠商負責人B等），避免同一題裡出現兩個小明。沒有具名當事人的情境（機關對機關、純程序性問題）不用硬塞名字。
- 題庫沒有隨機排序功能（之前有、後來應柯醒翰要求整個拿掉了），順序固定，方便討論題目時直接講「第N題」。
- Tab／分頁：`state[key]` 記錄每個題庫目前頁碼與資料，`PAGE_SIZE = 20`，`render(key)` 統一處理渲染，加新科目時同樣模式複製即可。
- header 只留標題＋右上角「柯醒翰製作」字樣，不要加介紹文字（之前被要求拿掉過）。

## 網站整體結構（加新科目時的頁面慣例）

`index.html` 是總覽 hub：每科一個 `.subject-group`（目前四科：行政程序法／方案管理／危機處理／貪污治罪條例與刑法），每科底下用卡片列出該科所有筆記頁面連結。新增科目時照抄現有 `.subject-group` 區塊、在最上方 `.jumpnav` 加一個錨點連結即可。

每科底下的頁面命名慣例（做新科目時比照）：
- `XX-考場抄寫骨幹.html`：答題架構公式＋速記表＋歷屆範文，給考前最後衝刺抄寫用。
- `XX-law.html` 或 `XX法條對照`：逐條法條原文＋白話解釋（全文範圍，見上面規則2）。
- `XX-textbook.html`：依官方教材整理的深讀筆記（保留學說、案例完整推理）。
- `XX模擬練習題`／案例分析：自製情境申論題，附示範解答。
- 情境速判練習（像本檔案）：選擇題形式的快速反應練習，非必備，目前只有行政程序法／貪污治罪條例與刑法兩科有做。

`exam-forecast.html` 是跨科的歷屆考題統計＋115年方向預測＋讀書優先順序頁，`歷屆試題`（108-114年逐年申論題解析連結）是該頁最後一個獨立區塊。`index.html` 的 jumpnav 也有一個直接連過去的連結。首頁 jumpnav 與行政程序法區塊之間有一個「意見回覆」大方塊，連到 Google 表單（填表人＋意見兩欄），回覆會進柯醒翰的 Google 試算表。
